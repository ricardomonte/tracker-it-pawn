import React from 'react';
import TrackerStyle from '../styles/Header.module.css';

const Header = () => (
  <div className={TrackerStyle.title}>
    <h1 className={TrackerStyle.text}>Track-It-Pawn</h1>
  </div>
);

export default Header;
